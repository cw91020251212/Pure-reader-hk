# 新假期 WeekendHK 圖片提取 — 破解報告

## 一、結論先講：問題已解決，唔需要瀏覽器、唔需要 Jina

我實測破解了 `imgs.weekendhk.com` 的網址規則，成功下載到**原圖**，
最大抓到 **5828 × 3885**（2.2 MB）—— 比網頁上顯示的縮圖大幾十倍。

| 測試文章 | 內容圖 | 失敗 | 最大原圖 |
|---|---|---|---|
| OneDegree 毛孩 | 3 張 | 0 | 2160 × 3840 |
| 週末出走澳門 | 9 張 | 0 | 5828 × 3885 |
| 八達通回贈優惠 | 1 張 | 0 | 602 × 293（該文只有 1 張） |

---

## 二、你（及之前幾個 AI）判斷錯了的地方

報告寫「Jina 把圖片濾走，來源數據根本沒有圖」。**這個結論不準確。**
我實測同一篇文章：

| 來源 | 抓到 `imgs.weekendhk.com` 網址數 |
|---|---|
| Jina (`r.jina.ai`) | **14 個** |
| 直接 `curl` 原站 | **105 個** |

Jina 並非「濾到一張都沒有」，而是**只剩零碎 14 個、且全部被降級**。
真正的死因有三個，跟你原本的推斷不同：

### 死因 1：`<picture>` 的 `<source srcset>` 才是真圖（這點你判斷對了）
實測該站每頁有 `<picture>` × 11、`<source>` × 45，
`src` 只放一個 `default.jpg` 佔位圖（全頁出現 5–13 次）。

### 死因 2（關鍵，之前無人發現）：CDN 有「寬度前綴」
真實網址長這樣：

```
https://imgs.weekendhk.com/322x/wp-content/uploads/2026/08/xxx-975x630.png.webp
                           ^^^^^ 這個前綴就是縮圖指令
```

`322x/`、`644x/`、`147x/`、`294x/` 是 CDN 即時縮放指令。
**把前綴刪掉，就直接拿到未縮放版本。** 實測：

| 網址 | 檔案大小 |
|---|---|
| `/322x/…` | 13 KB |
| `/644x/…` | 34 KB |
| `/1920x/…` | 138 KB |
| **刪掉前綴** | **59 KB → 再配合死因 3 可到 2.4 MB** |
| `/9999x/…` | 1.3 MB（CDN 真的會放大到 9999 px，但係插值放大，唔係原圖）|

### 死因 3（第二個關鍵）：檔名尾的 WordPress 尺寸後綴
```
xxx-975x630.png   ← 縮圖
xxx.png           ← 原圖
```
實測 `onedegree-janice…`：

| 網址 | 尺寸 | 大小 |
|---|---|---|
| `…-1024x768.jpeg` | 1024 × 768 | 89 KB |
| **`….jpeg`（刪後綴）** | **2048 × 1536** | **235 KB** |

另外 CDN 會加 `.webp` 轉檔尾（`xxx.png.webp`）和 `?v=1`，都要一併清掉。

### 所以完整還原公式是：
```
刪 /322x/ 前綴  +  刪 -975x630 後綴  +  刪 .webp 尾  +  刪 ?v=1  =  原圖
```

---

## 三、附上可即用工具：`weekendhk_image_grabber.py`

純 Python 標準庫 + Pillow，**不需要 headless browser、不需要 Jina、不受 lazy-load 影響**
（因為真網址本來就在 HTML 原始碼裡，只是被 Jina 的 Markdown 轉換丟掉了）。

```bash
python3 weekendhk_image_grabber.py "文章網址" -o 輸出資料夾
```

實際執行結果：
```
[1/4] 下載頁面 HTML …
      HTML 949,087 bytes  |  <picture>=11  <source>=45  default.jpg=13
[2/4] 解析圖片網址 …
      抓到 11 個候選縮圖網址  (已限定 <article> 內容區)
      去重後 9 張唯一圖片
[3/4] 下載原圖 (並行 6) …
[4/4] 完成 ✔
      成功 9 張 / 失敗 0 個
        001_dsc00898_…jpg    5828x3885    2289.0 KB
        002_dsc01362_…jpg    5363x3575    1197.1 KB
        003_dsc05091_…jpg    5585x3723    2008.9 KB
```

功能要點：
- **`<article>` 區塊定位** — 自動排除頁頭、頁尾、側欄、推薦位，只要文章內容圖。
  （不加這步會多抓 18 張 logo / App Store 徽章 / UI 箭嘴）
- **`--all`** 參數可連站台裝飾圖一齊抓。
- **原圖優先候選鏈** — 逐個試「最原始 → 次原始 → 原網址」，總有一個中。
- **MD5 去重** — 同一張圖多個尺寸只存一次。
- **`_manifest.json`** — 記錄每張圖的來源網址、尺寸、大小，失敗清單另列。
- 只用標準庫，`Pillow` 只用來讀尺寸（缺了也能跑，只是不顯示尺寸）。

---

## 四、GitHub 上相關資料（已幫你查證 star 數）

你問「可以上 GitHub 找相關資料嗎」—— 查了，但**先講重點：以上工具已解決你的問題，下面這些是後備方案**。

### 如果想要通用方案（非 WeekendHK 專用）

| 專案 | Star | 用途說明 |
|---|---|---|
| [`gildas-lormeau/SingleFile`](https://github.com/gildas-lormeau/SingleFile) | 22.1k | **最推薦給你**。瀏覽器擴充，把整頁（含所有已渲染圖片）存成單一 HTML。因為在瀏覽器內執行，lazy-load 已完成，這正是你報告第 3 節「最佳替代方案」的正解 |
| [`gildas-lormeau/single-file-cli`](https://github.com/gildas-lormeau/single-file-cli) | 1.5k | 上面那個的 CLI 版，可自動化批次處理 |
| [`Y2Z/monolith`](https://github.com/Y2Z/monolith) | 15.4k | Rust CLI，把網頁打包成單一 HTML（圖片轉 base64 內嵌）。但**它不執行 JS**，對 lazy-load 站效果有限 |
| [`ArchiveBox/ArchiveBox`](https://github.com/ArchiveBox/ArchiveBox) | 28.1k | 自架網頁存檔系統，同時輸出 HTML / PDF / 截圖 / WARC，多重保險 |
| [`mikf/gallery-dl`](https://github.com/mikf/gallery-dl) | 19.1k | 圖片批量下載器，支援上千站台。WeekendHK 未內建，但可自寫 extractor |
| [`unclecode/crawl4ai`](https://github.com/unclecode/crawl4ai) | 77.0k | LLM 導向爬蟲，內建 Playwright，可取代 Jina 做提取後端 |
| [`adbar/trafilatura`](https://github.com/adbar/trafilatura) | 6.4k | 正文提取，比 Readability 準，Python 可直接調用 |
| [`simonw/shot-scraper`](https://github.com/simonw/shot-scraper) | 2.5k | CLI 全頁截圖，對應你報告提到的「全頁截圖轉 PDF」方案 |
| [`opendatalab/MinerU-HTML`](https://github.com/opendatalab/MinerU-HTML) | 280 | 小模型驅動的 HTML 正文提取 |

---

## 五、給你的 App「PureRead HK」的具體修改建議

我讀了 `index.html`，發現架構上有兩個真正的問題：

### 問題 1：`normalizeMediaSources` 被定義了兩次
- 第 861 行：完整版（處理 8 種 `data-*` 屬性 + `srcset`）
- 第 2234 行：簡化版（只處理 4 種）

**後者會覆寫前者**（同一 scope 的函數宣告提升）。所以你辛苦寫的完整版
在第 475、1084、1196、1234、1260、1749 行的呼叫，實際上全部走了簡化版。
→ **刪掉第 2234 行那個簡化版**，這是最快見效的一步。

### 問題 2：架構上依賴 Jina，先天丟圖
`fetch('https://r.jina.ai/' + finalUrl)` 取 Markdown → `marked.parse()` 轉 HTML。
Markdown 格式**先天無法表達 `<picture>` / `srcset` / 多尺寸**，
所以無論前端正規化寫得多好，資訊在轉 Markdown 那一刻已經永久遺失。

**建議：改抓原始 HTML，不經 Markdown。** 你已有 DOMPurify，安全性沒問題：

```js
// 用支援 CORS 的純代理取原始 HTML（非 Markdown）
const res = await fetch('https://api.allorigins.win/raw?url=' + encodeURIComponent(finalUrl));
const html = await res.text();
const doc  = new DOMParser().parseFromString(html, 'text/html');
// 之後照樣 DOMPurify.sanitize + normalizeMediaSources
```
註：公共代理不穩定，正式用建議自架一個極簡 CORS proxy（Cloudflare Worker 十行搞定）。

### 問題 3：正規化邏輯要加「原圖還原」一步
這是你目前完全沒有的功能 —— 就算成功拿到 `srcset` 網址，
拿到的仍然是 `322x` 縮圖。加入這個函數：

```js
function toOriginalImageUrl(u) {
  if (!u) return u;
  u = u.split('?')[0].split('#')[0];          // 清 ?v=1
  u = u.replace(/\.(jpe?g|png)\.webp$/i, '.$1');  // 清 CDN .webp 轉檔尾
  u = u.replace(/^(https?:\/\/[^/]+\/)\d+x\//, '$1');       // 清 /322x/ 寬度前綴
  u = u.replace(/-\d{2,5}x\d{2,5}(?=\.[a-z0-9]{2,5}$)/i, ''); // 清 -975x630 後綴
  return u;
}
```
在 `normalizeMediaSources` 決定好 `bestSrc` / `bestSrcset` 之後，
對每個網址套一次這個函數即可。註意最後那條 regex 一定要求前面有 `-`，
否則會誤傷 `ww_logo350x100.png` 這種本身含尺寸的檔名（我實測撞過這個坑）。

---

## 六、對報告中其他判斷的核實

| 報告原文 | 核實結果 |
|---|---|
| 「`src` 始終指向 `default.jpg` 誤導提取器」 | ✅ 正確，實測每頁出現 5–13 次 |
| 「真實網址藏在 `srcset`，帶動態參數」 | ✅ 正確（動態參數是 `?v=1`，好清理） |
| 「Jina 把圖片網址全部濾掉」 | ❌ 不準確，實測仍留 14 個，但被降級成縮圖 |
| 「後端工具抓不到真圖，因為 lazy-load」 | ❌ 不是 lazy-load 的錯。真網址本來就在靜態 HTML 裡，`curl` 就抓到 105 個。真正問題是**沒有還原 CDN 縮圖前綴** |
| 「Linux 缺中文字體導致 PDF 亂碼」 | ✅ 這是真問題，但與圖片提取無關，裝 `fonts-noto-cjk` 即可 |
| 「瀏覽器列印 PDF 最穩定」 | 🔸 可行但非必要，而且**列印只會得到頁面上的縮圖，不是原圖**。想要原圖仍需上述網址還原 |

**最重要的一點**：你和之前的 AI 都把方向放在「如何讓瀏覽器渲染完成」，
但這個站的圖根本不需要渲染 —— 它們就在靜態 HTML 裡。
真正的門檻是**看懂 CDN 的縮圖網址語法**，而這一步純字串處理就做完了。
